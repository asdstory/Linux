# /etc/fstab: static file system information.
#
# Use 'blkid' to print the universally unique identifier for a
# device; this may be used with UUID= as a more robust way to name devices
# that works even if disks are added and removed. See fstab(5).
#
# <file system> <mount point>   <type>  <options>       <dump>  <pass>
/dev/mapper/ubuntu--vg-root /               ext4    errors=remount-ro 0       1
# /boot was on /dev/sda2 during installation
UUID=d6b16ca8-f483-491c-a140-d6961bdf8de6 /boot           ext4    defaults        0       2
# /boot/efi was on /dev/sda1 during installation
UUID=0C31-D378  /boot/efi       vfat    umask=0077      0       1
/dev/mapper/ubuntu--vg-swap_1 none            swap    sw              0       0

LABEL=CACHE_NVME       /scratch/nvme-ssd       btrfs   noatime,discard,ssd     0       1

# Tongyi's Biowulf data
//hpcdrive.nih.gov/data /data/biowulf-data-smb/dout2 cifs rw,sec=ntlmssp,username=dout2,domain=NIH.gov,vers=1.0,noauto,user 0 0

# The Jiang-Lab shared drive on NHLBI hl-share.nhlbi.nih.gov is automatically mounted, just type 

//hl-share.nhlbi.nih.gov /data/nhlbi-nfs rw,sec=ntlmssp,username=dout2,domain=nhlbi.nih.gov,vers=1.0,noauto,user 0 0

# The Jiang-Lab shared drive on NHLBI hl-share.nhlbi.nih.gov is automatically mounted, just type 

cd /data/nhlbi-nfs/lab-jiang

or cd to home, and you will see the lab-jiang directory. 
